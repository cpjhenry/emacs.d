#!/bin/bash
# daily-info-mode backend for Emacs
# 2024-01-10

cond_source() { [ -f $1 ] && . $1; }
cond_source $HOME/.bash_path
cond_source $HOME/.bash_routines

di
echo
calt |sed '$s/$/\n/'
ruled 4
alert -f
echo
calm $(date +%m)
