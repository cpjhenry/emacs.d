;; Assign to something other than the super key
;; on the mac, set right option as pass-thru, then use
;; standard mac keys to produce (ie. this code isn't needed)

(define-key key-translation-map (kbd "s--") (kbd "—")) ; em dash
(define-key key-translation-map (kbd "s-_") (kbd "–")) ; en dash
